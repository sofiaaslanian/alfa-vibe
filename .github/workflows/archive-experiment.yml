name: archive-experiment

on:
  push:
    branches: [experiment/rollback-group1]

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare snapshot
        run: |
          mkdir -p /tmp/alfa-vibe-rollback-group1
          rsync -a             --exclude='.git'             --exclude='.github/workflows/archive-experiment.yml'             ./ /tmp/alfa-vibe-rollback-group1/
      - name: Upload snapshot
        uses: actions/upload-artifact@v4
        with:
          name: alfa-vibe-rollback-group1
          path: /tmp/alfa-vibe-rollback-group1/
          include-hidden-files: true
          retention-days: 1
