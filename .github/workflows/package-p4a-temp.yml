name: package-p4a-temp

on:
  push:
    branches: [main]

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment/p4a-document-precision
      - name: Prepare snapshot
        run: |
          mkdir -p /tmp/alfa-vibe-p4a-document-precision
          rsync -a             --exclude='.git'             --exclude='.github/workflows/package-p4a-temp.yml'             ./ /tmp/alfa-vibe-p4a-document-precision/
      - name: Upload snapshot
        uses: actions/upload-artifact@v4
        with:
          name: alfa-vibe-p4a-document-precision
          path: /tmp/alfa-vibe-p4a-document-precision/
          include-hidden-files: true
          retention-days: 1
