name: package-snils-extension-temp

on:
  push:
    branches: [experiment/arch-v2-snils-extension]

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare snapshot
        run: |
          mkdir -p /tmp/alfa-vibe-arch-v2-snils-extension
          rsync -a             --exclude='.git'             --exclude='.github/workflows/package-snils-extension-temp.yml'             ./ /tmp/alfa-vibe-arch-v2-snils-extension/
      - name: Upload snapshot
        uses: actions/upload-artifact@v4
        with:
          name: alfa-vibe-arch-v2-snils-extension
          path: /tmp/alfa-vibe-arch-v2-snils-extension/
          include-hidden-files: true
          retention-days: 1
