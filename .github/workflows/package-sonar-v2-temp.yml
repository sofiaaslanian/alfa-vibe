name: package-sonar-v2-temp

on:
  push:
    branches: [experiment/arch-v2-sonar-cleanup-v2]

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare snapshot
        run: |
          mkdir -p /tmp/alfa-vibe-arch-v2-sonar-cleanup-v2
          rsync -a             --exclude='.git'             --exclude='.github/workflows/package-sonar-v2-temp.yml'             ./ /tmp/alfa-vibe-arch-v2-sonar-cleanup-v2/
      - name: Upload snapshot
        uses: actions/upload-artifact@v4
        with:
          name: alfa-vibe-arch-v2-sonar-cleanup-v2
          path: /tmp/alfa-vibe-arch-v2-sonar-cleanup-v2/
          include-hidden-files: true
          retention-days: 1
