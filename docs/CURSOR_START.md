# Задания Cursor

## Сейчас: Даня, этап 1

Поместите этот каталог `docs/` в корень проекта (уже лежит в репо). Вставьте следующий текст целиком в Agent. Не вставляйте отдельно прежние противоречащие документы.

```text
Мы вдвоём делаем хакатон AlfaGen PII. Сначала прочитай README.md, состояние git и файлы docs/START_HERE.md, docs/ARCHITECTURE.md, docs/DETECTION.md. Этот комплект — baseline v1; прежние архитектурные заметки не источник альтернативных решений. Если найдёшь официальный контракт организаторов, сравни его и выдели реальные противоречия. Не удаляй пользовательские изменения. Не мигрируй работающий стек ради архитектурной красоты.

Выполни только этап 1: работающий /process, state machine и автоматические тесты. Не предлагай ещё один архитектурный документ. Если репозиторий пустой, начни Python/FastAPI + Redis. Сначала проверь доступное окружение, Docker и совместимость зависимостей; закрепи проверенные версии. Для недоступных Docker/Redis сообщи точную ошибку и простое действие пользователю, не объявляй RAM проверкой Redis.

Зафиксируй интерфейсы Detector, MaskStrategy, StateStore, Restorer и finding contract. Реализуй /health, /ready, POST /process, dev_redact_v1, Redis live-state+seen-marker с атомарным созданием и TTL, HMAC, защищённое состояние. Следуй ARCHITECTURE.md. Для тестирования detection используй явный тестовый stub со spans, он не должен быть включён в сдаваемой конфигурации. Настоящие 17 detector'ов сейчас не делай. Если без detector приложение не готово для реальных запросов, /ready должен это честно показывать.

Проверь: mask→demask; оба retry; пустой payload и отсутствие ПД; unknown payload→409; 20 одновременных одинаковых запросов на двух API workers; два разных payload одного ID; TTL и tombstone→410; Redis unavailable→503; restart API с сохранённым state; Unicode/emoji; неизменность маски после изменения версии правил; отсутствие payload/PII/secrets в логах и ошибках. Проверка значения строки после JSON-декодирования, не буквального JSON encoding.

Не используй бизнес-LLM, не делай proxy/UI/fine-tuning/отдельный NER-сервис. Никаких реальных ПД и внешних сообщений. Не публикуй сервис без отдельного задания. Не перезаписывай .env с существующими секретами. Создай безопасный .env.example без ключей; ключи генерируй локально, не выводи в ответ.

В финале: список изменений; точные команды запуска; таблица выполненных тестов и фактических результатов; отдельно не запущенные тесты и причины; один следующий шаг. Остановись после этапа 1, чтобы мы передали твой отчёт в ChatGPT. Не утверждай, что официальный автотест или производительность подтверждены.
```

Готовность: именно выполненные тесты, включая настоящий Redis и два workers, а не только созданные файлы. Вернуться в ChatGPT с итоговым ответом Cursor; при блокере — с текстом ошибки. Повторно объяснять проект не нужно.

## Затем: Соня, этап 2

Даня передаёт Соне commit/состояние с зафиксированными интерфейсами. Соня работает в своей ветке, не меняет API/state.

```text
Прочитай docs/DETECTION.md и docs/ARCHITECTURE.md, существующие интерфейсы и docs/acceptance_cases.json. Реализуй detector layer baseline v1 без изменения /process и Redis state machine. Все 17 типов, ADDRESS форматно-контекстный; форматные rules; format-context parsers; локальный RuBERT NER (redmadrobot-rnd/rubert-base-pii-ner) + role rules и labelled-field extraction. Никакой внешней runtime LLM и fine-tuning. Зафиксируй реально проверенные версии и model id. Проверяй источники API библиотек по официальной документации.

Перенеси 68 cases в автоматические тесты без изменения expected spans/expected_result ради прохождения. Это target-type-only cases: enabled_types содержит только target_type. Не объявляй negative CARDHOLDER_NAME доказательством отсутствия PERSON_NAME. Для спорной разметки сохрани исходное ожидание и выведи отдельное предложение с причиной.

Реализуй all-types autotest_default_v1, eligibility, глобальные Unicode code-point offsets и resolver по документу. Добавь смешанный текст с ФИО+email+телефоном, два одинаковых email, метки серия/номер, emoji перед ПД, nested и crossing spans, entity/role на границе NER chunk, клиент с именем Александр Пушкин. Добавь обработку модели unavailable как 503 через существующий error contract. Test-only stub не должен включаться в сдаваемом режиме.

Запусти тесты. Отчитайся по каждому типу: passed/failed, exact span FP/FN, реальные проблемы, ограничение покрытия. Не выводи 68/68, если тесты не выполнены. Не меняй архитектуру и не начинай proxy. Верни краткий отчёт для ChatGPT.
```

## После detector: Даня, этап 3

```text
Интегрируй проверенный detector layer с /process по baseline v1. Запусти полный regression после интеграции. Затем добавь минимальный авторизованный proxy для одного текстового поля: server-side consumer allowlist, enabled_types/mask_types/demask_allowed, scoped tokens, отдельное namespace, текущая проверка прав перед восстановлением. Используй mock LLM в автоматических тестах; реальный провайдер — только из уже настроенных task-relevant credentials и без вывода секретов. Если провайдер не настроен, спроси только необходимый параметр, не подставляй произвольный внешний сервис.

Проверь изоляцию двух consumers, forbidden demask, переставленные/повторённые токены, чужие и повреждённые токены, отсутствие raw PII во всех отправляемых полях и tracing; store/detector failure блокируют вызов LLM. Не заявляй гарантию отсутствия всех ПД: detector может ошибаться. Реальный demo использует только синтетические данные. Остановись с отчётом о выполненных проверках для ChatGPT.
```

## Перед официальным прогоном: Даня, этап 4

Вручную через официальный канал уточнить: «Нужны точные примеры масок /process и правила сравнения, требования к доступу endpoint, максимальная задержка mask→demask и профиль нагрузки (размер текста/перцентиль/доли операций). Сейчас реализуем временную dev_redact_v1; как выполнить пробный прогон?» С ответом вернуться в ChatGPT. Отправка сообщения этим комплектом не выполнена.

## После интеграции: Даня, этап 5

```text
Прочитай performance section ARCHITECTURE.md. Создай и выполни воспроизводимый correctness/нагрузочный прогон на синтетических данных. Сначала новый holdout Сони отдельно от 68 открытых fixtures. Затем профили 2 КБ/32 КБ/256 КБ и отдельный payload 100 000 токенов с явно указанным tokenizer. Наращивай нагрузку до насыщения, не обещай 1000 RPS заранее. Не нагружай внешний сервис и production endpoint без отдельного разрешения; нагрузка только на локально запущенный проект.

Фиксируй hardware/workers/input bytes/chars/tokens, долю unique/retry/demask, успешный RPS, p50/p95/p99, 429/5xx, CPU/RAM включая Redis. Не считай cache retries тестом detector throughput. Контролируй память и прекращай при опасном росте/исчерпании ресурсов. Подготовь короткий README запуска и честный отчёт: что проверено, что нет, официальная совместимость маски. Верни результат в ChatGPT для подготовки защиты.
```
