# Текущее состояние

Файл для первого чтения в Kilo Code. Код не переписывать, пока расхождение документации и репозитория не проверено по файлам.

## Repository

- Ветка baseline: `main`
- Полный SHA: `e79f468918b5a2ef73b2355a32548e5c290d15eb`
- Short SHA: `e79f468`
- Сообщение: `Remove placeholder-free f-strings and record how the solution was built.`
- Время: `2026-09-23 17:52:53 +0300`
- На момент снятия baseline локальных незакоммиченных изменений не было, `HEAD` совпадал с `origin/main`.

## Current goal

Максимально пройти проверку AlfaGen. Целевой score качества кода — 10000. Этот коммит на платформу ещё не загружали.

## Working

- Публичный стенд `http://135.106.220.67`: `GET /ready` возвращает `{"status":"ready"}`, `GET /health` возвращает `status=ok`, 5 систем, 17 типов, storage `redis`. Проверено в сессии handoff.
- `POST /process` на этом стенде раньше проходил цикл маска → исходный текст на фейковом email. В сессии handoff заново не гонялся.
- Профиль `autotest` не включает context ML (`config.yaml`).
- Демо-система ML включает. Вызов LLM из `/process` не делается.

## Tests

- `pytest -q` на `e79f468`: `383 passed, 1 skipped`.
- `NER_ENABLED=0 python scripts/eval_acceptance.py`: `67/68`, roundtrip `1.0`, провал `DRIVER_LICENSE_NUMBER_1`. Локальный HTTP внутри скрипта не был поднят.
- Замер Selectel 23.09.2026, 4 ядра, без GPU, до коммита очистки f-string, на том же алгоритме `/process`: при цели 1000 RPS успешных около 960–970, p95 успешных около 8 мс. 2000 RPS не достигнуты. Текст около 480 тысяч символов: HTTP 200 за 1,9 с. Повторный замер после `e79f468` не делался.

## Known issues

- Score платформы на ближайшем предыдущем `main` — 9927, не 10000. Состав того лога: 0 blocker, 1 critical, 12 major, 1 minor. Имена правил платформа не отдала.
- Внутренний прогон 68 кейсов: 67/68.
- README в таблице систем пишет, что `autotest` использует RuBERT. `config.yaml` ставит `use_context_ml: false`.
- `.env.example` содержит `PROCESS_CONCURRENCY=180`. `docker-compose.yml` по умолчанию подставляет `2`.
- `ui/icons.zip` и `ui/photos.zip` лежат в git. Условие zip просит не упаковывать объёмные архивы и медиа.
- 2000 RPS при задержке до 1 секунды на этом VPS не получены.

## Risks

- Скрытый датасет может требовать контекстные спаны, которые без NER не находятся.
- `409` на чужой текст с известным `payload_id` может дать пять невалидных ответов подряд.
- Падение NER-sidecar не пускает proxy, хотя `/process` модель не вызывает.
- Повторная загрузка zip может не добрать баллы до 10000: точный набор правил AlfaGen в репозитории не воспроизведён.

## Do not change casually

- Форму `POST /process`: поля `payload`, `payload_id`, ответ `result`. Не добавлять обязательные поля и не требовать ключ.
- Не вызывать LLM из `/process`.
- Не логировать исходный текст и значения ПД.
- Не менять `mask_style` автотеста и формат звёздочек, пока нет их эталона.
- Не ослаблять атомарность `payload_id` и шифрование состояния.
- Не включать context ML на `autotest`, не измерив заново RPS и 68 кейсов.

## Next recommended step

Прочитать `docs/ai-context/` и сверить с кодом. Ничего не менять в первом проходе. Вернуть противоречия и риск для скрытых тестов со ссылкой на файл. Первый кандидат на правку после этого — расхождение README и `config.yaml` про RuBERT в `autotest`, затем точечные замечания анализатора без смены контракта.
