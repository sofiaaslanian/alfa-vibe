# Стартовый промпт для Kilo Code / DeepSeek Flash

Вставить в новый чат целиком. Репозиторий уже должен быть открыт на ветке `chore/deepseek-handoff` после её появления, либо на `main` коммита `e79f468`, если ветки ещё нет. Код не переписывать.

```text
Ты продолжаешь submission хакатона Alfa-Bank в репозитории alfa-vibe.
Существующий код не переписывай и в этом первом проходе не меняй ни одного файла.

Сначала прочитай все файлы в docs/ai-context/:
00_PROJECT.md, 01_ARCHITECTURE.md, 02_CODEBASE_MAP.md, 03_ALFAGEN.md, 04_TESTING.md, 05_DECISIONS.md, 06_CURRENT_STATE.md.
Потом открой реальный код и проверь, не разошлась ли эта папка с реализацией.

Ограничения:
- POST /process принимает только payload и payload_id и возвращает result.
- Из /process нельзя вызывать LLM и нельзя требовать API-ключ.
- Не логируй персональные данные и исходный текст.
- Не включай context ML на профиле autotest, пока отдельно не измерены RPS и внутренние 68 кейсов.
- Организаторы разрешили сдавать текущее решение. Объём ChatGPT и Cursor уже указан в README. Проценты не выдумывай.

Верни только анализ, без патча:

1. Как ты понял задачу и что такое AlfaGen в этом репозитории.
2. Фактическая архитектура.
3. Критичные файлы.
4. Контракты API, которые нельзя ломать.
5. Потенциальные дефекты.
6. Риск для скрытого датасета и нагрузочного прогона AlfaSonar.
7. Противоречия между docs/ai-context, README и кодом.
8. Что тебе всё ещё неизвестно.

Каждое существенное утверждение сопроводи путём файла. Если подтверждения в репозитории нет, так и напиши.
```
